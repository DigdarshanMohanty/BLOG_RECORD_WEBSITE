<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen bg-zinc-800 text-white">
    <%- include('partials/header') %>
    <div class="container mx-auto px-4 py-8 text-center flex flex-col items-center justify-center flex-grow"> {/* Changed min-h-[calc(100vh-150px)] to flex-grow */}
        <h1 class="text-3xl font-bold text-red-500 mb-4 font-heading">Oops! Something went wrong.</h1> {/* Added font-heading */}
        <% if (typeof message !== 'undefined' && message && message.length > 0) { %>
            <p class="text-xl mb-6"><%= message %></p>
        <% } %>
        <!-- Flash messages (error_msg) will be displayed by the header -->
        <a href="/" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
            Go Back Home
        </a>
    </div>
<%- include('partials/footer') %>
</body>
</html>
