<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen bg-zinc-800 text-white">
     <%- include('partials/header') %>
     <div class="w-full p-10 flex-grow"> {/* Changed min-h-screen to flex-grow, removed bg/text as on body */}
        <h4 class="text-2xl mb-5 font-heading">Create Your Blog</h4> {/* Added font-heading */}
      <form action="/create" method="post" onsubmit="return validateForm();">
         <div class="flex flex-wrap gap-3 w-full md:w-2/3">    {/* Removed text-white, changed w-2/3 to w-full md:w-2/3 */}
               <input type="text" name="title" placeholder="Title" class="block w-full h-10 rounded-md p-2 bg-zinc-600 text-white outline-none mb-4">
               <textarea class="block w-full outline-none rounded-md resize-none bg-zinc-600 p-2 min-h-40 mb-4" placeholder="Blog Description" name="description"></textarea>
               <input type="url" name="imageUrl" placeholder="Image URL (optional)" class="block w-full h-10 rounded-md p-2 bg-zinc-600 text-white outline-none mb-4">
               <input type="submit" class="bg-blue-500 text-white rounded-md p-2 font-bold mt-4" value="Create Blog">
         </div>
      </form>
     </div>
     <script src="/javascripts/formValidation.js"></script>
<%- include('partials/footer') %>
</body>
</html>