<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="bg-zinc-800 w-full h-screen text-white">
    <center><h4 class="inline-block text-xl font-bold border border-4 p-3 m-3">BLOG AREA</h4></center>
      <% if(blogs.length === 0){ %>  
      <div class="main flex gap-5 w-full h-2/3 justify-center items-center">  
      <div class="flex flex-col items-center justify-center bg-zinc-500 w-1/3 p-3 rounded-md gap-3 h-1/3">
         <div class="flex text-md italic items-center justify-center font-bold">You have no Blog's currently!</div>
         <button class="text-white bg-blue-500 px-4 py-2 rounded-lg" type="submit">
          <a href="/create">Create a Blog</a>
         </button>
      </div> 
      </div>
    <% } else { %>
      <div class="main flex gap-2 w-fit mt-5 h-3/4 justify-center items-center bg-zinc-700 flex-col p-3 rounded-md m-5">
        <div>
        <button class="text-white bg-blue-500 p-2 rounded-md" type="submit">
          <a href="/create">Create a Blog</a>
          </button>
        </div>
        <div class="flex justify-center items-center p-3 gap-3 h-full text-white w-fit flex-wrap">
        <% blogs.forEach((blog, index) => { %>
           <div class="card flex flex-col gap-3 bg-zinc-600 rounded-lg p-3 w-full sm:w-1/2 md:w-1/3 lg:w-1/4 xl:w-1/5 h-fit  justify-center">
            <h1 class="text-xl italic font-bold text-red-600 line-clamp-1"><%= blog.title %></h1>
            <p id="desc-<%= index %>" class="text-md italic line-clamp-6"><%= blog.description %></p>
            <a href="/blog/<%= blog._id %>" class="block text-blue-500 text-md italic mt-4">Read More...</a>

            <div class="flex gap-2">
            <button class="block bg-red-500 text-md p-1 rounded-md flex justify-center w-1/2"><a href="/delete/<%= blog._id %>">Delete</a></button>
            <button class="block bg-green-500 text-md p-1 rounded-md flex w-1/2 justify-center"><a href="/edit/<%= blog._id %>">Edit</a></button>
            </div>
           </div>
        <% }) %>
        </div>
      </div>      
    <% } %>
    </div> 
</body>
</html>
